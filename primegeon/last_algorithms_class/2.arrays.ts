const a = [];
console.log(a);
/**
 * Array = Contigous memory space
 * we can walk that space we start from the 0th mem address
 * for each iteration offset by the mem space and add one byte address
 */
